const api = {
    url: `https://random-students.herokuapp.com/${prefix}/${version}/${resources}`, // API endpoint
    prefix: "api",
    version: "v1",
    resources: "students",
    key: "",
}

export default api;